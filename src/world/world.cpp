
#include "world.h"
/*
define block class:
enum for block type (grass,stone,dirt,air)
grass is only on surface dirt and stone are diffrent air has to be above

chunk class
16X16X16 for blocks
X  Y  Z

Y 6 just grass blocks
Y > 6 just air blocks
Y < 6 mix of dirt and stone

*/

class Chunck
{
private:
    //std::vector<std::vector<std::vector<cubeMesh>>> Chunck(16, std::vector<std::vector<cubeMesh>>(16, std::vector<cubeMesh>(16)));
};
